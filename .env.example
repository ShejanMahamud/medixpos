# Polar.sh Configuration
# Get your Organization ID from: https://polar.sh/dashboard/[your-org]/settings
POLAR_ORG_ID=

# Environment
NODE_ENV=

# Windows Code Signing (Optional - only needed for production signed builds)
# Leave empty or unset for unsigned development builds
WIN_CSC_LINK=
WIN_CSC_KEY_PASSWORD=

# To build without signing (development):
# pnpm build:win

# To build with signing (production):
# 1. Get a code signing certificate from a trusted CA (DigiCert, Sectigo, etc.)
# 2. Set WIN_CSC_LINK to the path of your .pfx certificate file
# 3. Set WIN_CSC_KEY_PASSWORD to your certificate password
# 4. Run: pnpm build:win:signed
